services:
  db:
    image: postgres:16
    container_name: pg-practica1
    restart: always
    environment:
      POSTGRES_PASSWORD: practica1
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: